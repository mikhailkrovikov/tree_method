<Window x:Class="TreeMethod.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TreeMethod.Helpers"
        mc:Ignorable="d"
        Title="И-ИЛИ Дерево"
        Height="1080" Width="1920"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        ResizeMode="NoResize">

    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>

    <DockPanel>

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Файл">
                <MenuItem Header="Новый проект" Command="{Binding NewProjectCommand}" />
                <MenuItem Header="Открыть..." Command="{Binding OpenCommand}" />
                <MenuItem Header="Сохранить" Command="{Binding SaveCommand}" />
                <MenuItem Header="Сохранить как..." Command="{Binding SaveAsCommand}" />
                <Separator />
                <MenuItem Header="Выход" Command="{Binding ExitCommand}" />
            </MenuItem>

            <MenuItem Header="_Вид">
                <MenuItem Header="Дерево" Command="{Binding ShowTreePageCommand}" />
                <MenuItem Header="Матрицы" Command="{Binding ShowMatricesPageCommand}" />
                <MenuItem Header="Расчёт" Command="{Binding ShowCalculationPageCommand}" />
                <MenuItem Header="Результаты" Command="{Binding ShowResultsPageCommand}" />
            </MenuItem>

            <MenuItem Header="_Справка">
                <MenuItem Header="О программе" Command="{Binding AboutCommand}" />
            </MenuItem>
        </Menu>


        <ToolBar DockPanel.Dock="Top" Padding="4">
            <Button Content="➕ Новый" Command="{Binding NewProjectCommand}" Margin="2" />
            <Button Content="📂 Открыть" Command="{Binding OpenCommand}" Margin="2" />
            <Button Content="💾 Сохранить" Command="{Binding SaveCommand}" Margin="2" />
            <Separator />
            <Button Content="▶ Запуск расчёта" Command="{Binding RunCalculationCommand}" Margin="2" />
            <Button Content="🖼 Экспорт дерева" Command="{Binding ExportTreeCommand}" Margin="2" />
        </ToolBar>


        <Frame x:Name="MainFrame"
       NavigationUIVisibility="Hidden"
       Content="{Binding CurrentPage}"
       JournalOwnership="OwnsJournal" />

        <!-- Строка состояния -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar Width="150"
                     Height="16"
                     IsIndeterminate="True"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" />
            </StatusBarItem>
        </StatusBar>
    </DockPanel>
</Window>
