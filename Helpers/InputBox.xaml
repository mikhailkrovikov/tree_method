<Window x:Class="TreeMethod.Helpers.InputBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Переименование"
        Height="140" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Margin="0,0,0,8"/>
        <TextBox x:Name="ValueBox" Grid.Row="1" Margin="0,0,0,8"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="70" Margin="0,0,8,0" Click="Ok_Click"/>
            <Button Content="Отмена" Width="70" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
